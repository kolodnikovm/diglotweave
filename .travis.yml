language: python
python:
  - "3.6"
  - "nightly"
virtualenv:
    system_site_package: true
install: 
  pip install -r requirements.txt
script:
  coverage run ./TextFormatter/main.py --test
script:
  ls -la
script:
  coverage report

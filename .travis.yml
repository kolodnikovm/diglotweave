language: python
python:
  - "3.6"
  - "nightly"
virtualenv:
    system_site_package: true
install: 
  pip install -r requirements.txt
script:
  cd ./TextFormatter/
script:
  coverage run main.py --test
script:
  coverage report
